<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>高瀚宇生日蛋糕</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: "Microsoft YaHei", sans-serif;
      background: linear-gradient(135deg, #f5e6ff, #e0d7ff);
      text-align: center;
      color: #333;
    }

    .container {
      max-width: 800px;
      margin: 40px auto;
      padding: 20px;
      border-radius: 20px;
      background: white;
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    }

    h1 {
      color: #d32f2f;
      margin-bottom: 20px;
    }

    .cake-area {
      position: relative;
      width: 400px;
      height: 400px;
      margin: 20px auto;
      background: url('https://via.placeholder.com/400x400/f8c9e0/ffffff?text=蛋糕底胚') no-repeat center;
      background-size: cover;
      border-radius: 50%;
    }

    .layer {
      position: absolute;
      width: 80px;
      height: 80px;
      background: url('https://via.placeholder.com/80x80/fdf7f7/ff6b6b?text=奶油') no-repeat center;
      background-size: cover;
      border-radius: 50%;
      cursor: pointer;
      transition: transform 0.3s ease;
    }

    .layer:hover {
      transform: scale(1.1);
    }

    .btn {
      padding: 12px 30px;
      background: #d32f2f;
      color: white;
      border: none;
      border-radius: 50px;
      font-size: 18px;
      cursor: pointer;
      margin: 20px 0;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

    .btn:hover {
      background: #b71c1c;
    }

    .result {
      display: none;
      margin-top: 40px;
      font-size: 48px;
      color: #d32f2f;
      font-weight: bold;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
      font-family: 'STXingkai', 'KaiTi', serif;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🎂 为高瀚宇做生日蛋糕！</h1>
    <div class="cake-area" id="cake"></div>

    <button class="btn" onclick="addLayer()">加一层奶油</button>
    <button class="btn" onclick="finish()">完成蛋糕！</button>

    <div class="result" id="result">高瀚宇，生日快乐！</div>
  </div>

  <script>
    const cake = document.getElementById("cake");
    const result = document.getElementById("result");

    function addLayer() {
      const layer = document.createElement("div");
      layer.className = "layer";
      layer.style.left = Math.random() * 300 + 50 + "px";
      layer.style.top = Math.random() * 300 + 50 + "px";
      cake.appendChild(layer);
    }

    function finish() {
      document.querySelector(".btn").style.display = "none";
      result.style.display = "block";
    }
  </script>
</body>
</html>